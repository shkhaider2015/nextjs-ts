import styled from "styled-components";

const RegisterWrapper = styled.div`
   min-height: 89.5vh;
   min-width: 100vw;
   overflow-x: hidden;
   overflow-y: auto;
   display: grid;
   place-items: center;
`

export default RegisterWrapper;